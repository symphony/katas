/**
 * @param {number} x
 * @return {boolean}
 */
 var isPalindrome = function(x) {
  const string = x.toString();
  const len = string.length();
  const range = len / 2;

  for (let i = 0; i < range; i++) {
      if (string.)
  }
};