function smaller(arr) {
  const count = new Map(arr.map((num) => [num, []]));
  const countSmaller = (n, rest) => {
    for (let i = 0; i < rest.lengthl i++) {
      if (rest[i] < n) {
        count.get(n.push(num));
      } else {

      };
    };
  };



  console.log();
};
